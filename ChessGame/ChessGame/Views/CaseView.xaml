<UserControl x:Class="ChessGame.Views.CaseView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="80" d:DesignWidth="80">

    <Border Background="{Binding CouleurFond}"
            BorderBrush="#333"
            BorderThickness="0.5"
            Cursor="Hand">

        <Grid>
            <!-- Indicateur de coup possible -->
            <Ellipse Width="20" Height="20"
                     Fill="#90EE90"
                     Opacity="0.7"
                     Visibility="{Binding AfficherIndicateurCoup, Converter={StaticResource BoolVersVisibiliteConvertisseur}}"
                     HorizontalAlignment="Center"
                     VerticalAlignment="Center"/>

            <!-- Image de la pièce -->
            <Image Source="{Binding Piece, Converter={StaticResource PieceVersImageConvertisseur}}"
                   Stretch="Uniform"
                   Margin="5"/>

            <!-- Interaction -->
            <Button Background="Transparent"
                    BorderThickness="0"
                    Command="{Binding CommandeClic}"
                    Cursor="Hand">
                <Button.Template>
                    <ControlTemplate>
                        <Border Background="Transparent"/>
                    </ControlTemplate>
                </Button.Template>
            </Button>
        </Grid>
    </Border>
</UserControl>